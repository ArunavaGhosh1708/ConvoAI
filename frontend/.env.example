# Backend base URL (leave empty to use Vite proxy in dev)
VITE_API_URL=

# API key for the chat endpoint (must match backend API_KEY)
VITE_API_KEY=dev-api-key

# Admin JWT token (generate with: python -m app.utils.gen_token --role admin)
VITE_ADMIN_JWT=
